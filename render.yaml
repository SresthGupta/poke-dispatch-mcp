services:
  - type: web
    name: poke-dispatch-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.server
    envVars:
      - key: RENDER
        value: "true"
      - key: PYTHON_VERSION
        value: "3.12.0"
