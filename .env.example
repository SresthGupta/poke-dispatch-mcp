# Port for the MCP server (default: 8000)
PORT=8000

# Workspace directory for file operations (default: ~/poke-workspace)
WORKSPACE_DIR=~/poke-workspace

# Notes directory (default: ~/poke-workspace/notes)
NOTES_DIR=~/poke-workspace/notes
